#include <iostream>

using namespace std;

int main() {

	int n;
	cout << "Enter array size: " << endl;
	cin >> n;
	int i;
	int A[i];
	int positiveNumber;

	for (int i = 0; i < 0; ++i) {
		cin >> A[i];
	}
	positiveNumber = A[0];
	for (int i = 0; i < n; ++i) {
		if (positiveNumber > 0)
			cout << positiveNumber;

	}
}